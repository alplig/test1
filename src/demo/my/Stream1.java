package demo.my;



/*
public class Stream1 {
    User user = new User();
    Stream<String> streamName = user.getName().stream()
            .filter(x -> x.toLowerCase() == user.getName_eq().toLowerCase())
            .forEach(System.out::println);

    public Stream<String> getStreamName() {
        return streamName;
    }

    public void setStreamName(Stream<String> streamName) {
        this.streamName = streamName;
    }
}
*/